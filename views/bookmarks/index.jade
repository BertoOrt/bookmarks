extends ../layout

block content
  main
    .container
      h2 My bookmarks
      if bookmarks
        for bookmark in bookmarks
          p
            a(href='/users/'+userId+'/bookmarks/'+ bookmark._id) !{bookmark.name}
      if userId
        a.button.show(href='/users/'+ userId + '/bookmarks/new') New
  img.image(src='/images/gum.png')  
