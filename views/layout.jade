doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js")
  body
    header
      .container
        .header
          .title
            a.left(href='/') bookmarks
          .nav
            if !cookieId
              .col.right
                a(href='/login') login
              .col.right
                a(href='/signup') signup
              .col.right
                a(href='/categories') browse
            else
              .col.right
                a(href='/logout') logout
              .col.right
                a(href='/users/'+ cookieId) my favs 
              .col.right
                a(href='/users/'+ cookieId + '/bookmarks') my bookmarks 
              .col.right
                a(href='/categories') browse
      .subbanner
    block content
    footer
      .container
  script(src="/javascripts/client.js")
